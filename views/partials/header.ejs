<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] overflow-hidden">
    <nav class="w-full flex justify-between items-center px-10 py-4 bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400">
        <h3 class="text-2xl font-bold text-white">Cartify</h3>
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
        <% if(loggedin) { %>
            <div class="flex gap-5">
                <a href="/shop" class="text-white hover:text-blue-200 transition-colors">Shop</a>
                <a href="/cart" class="text-white hover:text-blue-200 transition-colors">Cart</a>
                <a href="/shop" class="text-white hover:text-blue-200 transition-colors">My Account</a> 
                <a class="text-red-600 hover:text-red-900 transition-colors" href="/users/logout">Logout</a>
            </div>
        <% } %>
    </nav>
